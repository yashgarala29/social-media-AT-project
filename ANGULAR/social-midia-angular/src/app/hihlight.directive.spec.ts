import { HihlightDirective } from './hihlight.directive';

describe('HihlightDirective', () => {
  it('should create an instance', () => {
    const directive = new HihlightDirective();
    expect(directive).toBeTruthy();
  });
});
